# 你的 X auth_token cookie（浏览器登录 x.com 后，开发者工具 → Application → Cookies → auth_token）
AUTH_TOKEN=

# HTTP 服务端口
PORT=8090

# 可选：调用方需带的访问令牌（?key= 或 Authorization: Bearer）。留空则不校验
PLUME_API_KEY=

# 可选：默认抓取条数
PLUME_DEFAULT_LIMIT=40
